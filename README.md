# geakr-v1
GEAkr is a lightweight project reference folder pattern. The chatbot uses the selected project folder as context, so answers are grounded in the project’s registered references rather than general memory alone.
